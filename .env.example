# The static production origin and crawl policy are source-controlled in
# src/lib/site-config.ts: https://msglove.kr

# GA4 web data-stream ID. Leave blank to omit the Google tag entirely.
# Use this exact key in Netlify Site configuration > Environment variables.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
